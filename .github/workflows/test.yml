name: Test
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Flex and Bison
        run: |
          md ..\flex-bison
          cd ..\flex-bison
          curl.exe -L -O "https://github.com/lexxmark/winflexbison/releases/download/v2.5.25/win_flex_bison-2.5.25.zip"
          unzip win_flex_bison-2.5.25
      - uses: actions/checkout@v3
        with:
          repository: gatk555/ngspice
          ref: master
      - uses: microsoft/setup-msbuild@v1.1
      - name: Build
        shell: cmd
        run: |
          set config=Release
          set plat=Win32
          set suffix=""
          set opt=-property:Configuration=%config% -property:Platform=%plat%
          set
          echo command msbuild %opts% visualc\xspice\cmpp\cmpp.vcxproj
          msbuild %opts% visualc\xspice\cmpp\cmpp.vcxproj
          msbuild %opts% visualc\xspice\analog.vcxproj
          msbuild %opts% visualc\xspice\digital.vcxproj
          msbuild %opts% visualc\xspice\spice2poly.vcxproj
          msbuild %opts% visualc\xspice\table.vcxproj
          msbuild %opts% visualc\xspice\xtradev.vcxproj
          msbuild %opts% visualc\xspice\xtraevt.vcxproj
          set cms=codemodels\%plat%\%config%
          md visualc\%cms%
          move visualc\xspice\%cms%\*.cm visualc\%cms%
          dir visualc\%cms%
          msbuild %opts% visualc\vngspice.vcxproj
          xcopy /s /i examples c:\Spice%suffix%/share/examples
      - name: Save
        uses: actions/upload-artifact@v3
        with:
          name: Ngspice-41, Windows 32-bit
          path: C:\Spice
