name: Test Homebrew env
on:
  workflow_dispatch:

jobs:
  check-darwin-x64:
    # this is currently macos-11, Big Sur
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Dependencies
        run: | 
          brew install make libelf freeglut patchelf 
          brew tap osx-cross/avr
          brew install avr-gcc@8 avr-binutils
      - name: Test
        run: |
          set
          echo _____ Environment ______
          env
          echo _____ Files ______
          ls -R /usr/local/opt/avr-gcc@8
          ls -R /usr/local/opt/avr-binutils
          echo which avr-gcc
          export PATH="/usr/local/opt/avr-gcc@8/bin:$PATH"
          which avr-gcc

